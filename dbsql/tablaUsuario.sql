CREATE TABLE  usuario(
    id int auto_increment PRIMARY KEY,
    name varchar(255),
    family_name varchar(255),
    nickname varchar(255),
    email varchar(255),
    pass varchar(255),
    address varchar(255)
);